{
  "name": "@changes-page/ui",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "rimraf dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "classnames": "^2.3.1",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": ">=18",
    "rimraf": "^5.0.1",
    "typescript": "^4.9.5"
  }
}
